import $ from '../core';

$.prototype.accordion = function (headActive = 'accordion-head-active',
   contentActive = 'accordion-content--active', paddings = 40) {
   for (let i = 0; i < this.length; i++) {
      $(this[i]).click(() => {

      });
   }
};